<% title @gallery.name %>

<% image_table(@gallery.images, @columns) do |image| %>
  <%= link_to(image_tag(image.upload.url(:thumb)), image.upload.url(:large), :rel => "group", :class => "colorbox", :title => image.description) %>
<% end %>
